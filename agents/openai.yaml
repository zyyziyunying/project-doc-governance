interface:
  display_name: "Project Doc Governance"
  short_description: "Place docs with local rules first"
  default_prompt: "Use $project-doc-governance to place, split, or reorganize project docs using repository-local rules first, then the fallback design/check/plan/status/problem/discussion/product taxonomy and header template if needed."
